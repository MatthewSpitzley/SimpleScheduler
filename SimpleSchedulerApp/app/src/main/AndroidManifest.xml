<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.simpleschedulerproject">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SimpleSchedulerProject">
        <activity
            android:name=".ProfileActivity"
            android:label="Profile Information"/>
        <activity
            android:name=".GoogleSignInActivity"
            android:label="Google Sign In" />
        <activity
            android:name=".LauncherFakeActivity"
            android:label="LauncherSystem">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TaskList"
            android:label="Task List" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CalendarScreen"
            android:label="Calendar" />
        <activity
            android:name=".History"
            android:label="History"/>
        <activity
            android:name=".SettingsActivity"
            android:label="Settings" />


    </application>

</manifest>